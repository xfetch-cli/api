[package]
name = "xfetch-guest-api"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Host-call helpers for xfetch WebAssembly guests"
repository = "https://github.com/xfetch-cli/api"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
